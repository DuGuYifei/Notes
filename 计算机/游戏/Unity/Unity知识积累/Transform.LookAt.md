# Transform.LookAt

```csharp
transform.LookAt(target,transform.right); // x轴朝向目标
transform.LookAt(target,-transform.right);// -x轴朝向目标
transform.LookAt(target,transform.up);    // y轴朝向目标
transform.LookAt(target,-transform.up);   // -y轴朝向目标
```